import {
  formatText
} from 'ember-cli-helpers/helpers/format-text';

module('FormatTextHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatText(42);
  ok(result);
});
